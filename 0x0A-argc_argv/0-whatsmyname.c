#include <stdio.h>

/**
 * main - prints name of program
 * @argc: number of arguments passed
 * @argv: array of arguments passed
 *
 * Return: always zero
 */
int main(argc, argv[])
{
	printf(argv[0]);

	return (0);

}
