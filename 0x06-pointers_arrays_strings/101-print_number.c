#include <stdio.h>

/**
 * print_number - prints integers
 * @value: integer to be printed
 *
 * no return
 */
void print_number(int n)
{

	if(value != 0)
	{

		print_number(value/10);

		putchar((value%10) + '0');

	}
}
